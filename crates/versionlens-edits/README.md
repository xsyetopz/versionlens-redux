# versionlens-edits

Edit construction for dependency updates, including version replacement and dependency sorting where a manifest format supports safe ordering.

Validation command: `cargo test -p versionlens-edits`.

License: [ISC](../../LICENSE).
